apiVersion: v1
kind: Service
metadata:
  name: infra-orch-web
  labels:
    app.kubernetes.io/name: infra-orch-web
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: infra-orch-web
  ports:
    - name: http
      port: 80
      targetPort: http
