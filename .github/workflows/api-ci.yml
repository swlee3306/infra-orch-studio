name: api-ci

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain public repository boundary
        run: echo "Deployment is disabled in this public example repository. No images or manifests are published."
