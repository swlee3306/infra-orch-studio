# Example secret. Replace values before production use.
apiVersion: v1
kind: Secret
metadata:
  name: openstack-clouds
  namespace: infra
type: Opaque
stringData:
  clouds.yaml: |
    clouds:
      exporter-internal:
        auth:
          auth_url: https://openstack.example.invalid:5000/v3
          username: replace-me
          password: replace-me
          project_name: replace-me
          user_domain_name: Default
          project_domain_name: Default
        region_name: RegionOne
        interface: public
        identity_api_version: 3
